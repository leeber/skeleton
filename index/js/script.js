

//This anonymous function calls itself, and generates the entire script inside the function's 
//////scope, to prevent accidental overwriting of data in the "Window" Global Scope
(function( window, undefined ){

	//javascript code goes here

} )( window, undefined );

